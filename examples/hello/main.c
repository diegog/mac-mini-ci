/* The smallest thing worth signing: proves the release lane end to end. */
#include <stdio.h>

int main(void) {
    puts("hello from a notarized binary built on a Mac mini on a shelf");
    return 0;
}
